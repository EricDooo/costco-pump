# Copy to .env (gitignored) and fill in real values. Referenced by
# docker-compose.yml via ${...} interpolation.

POSTGRES_PASSWORD=

# Free key from https://www.eia.gov/opendata/register.php -- worker.py's
# refresh_benchmarks job (national/PADD-region gas prices + WTI crude spot,
# see api/app/scraper/eia.py). Not a scraping credential, just a public
# government API's rate-limit identifier.
EIA_API_KEY=
